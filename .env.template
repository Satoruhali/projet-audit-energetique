PORT=3000
BASE_URL=
JWT_SECRET=
JWT_EXPIRE=7d

# MySQL
DB_HOST=localhost
DB_PORT=3306
DB_NAME=audit_energetique
DB_USER=root
DB_PASS=
DB_LOG=false

# SMTP (nodemailer) — Gmail (mot de passe d'application)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=

# Rate limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=100
RATE_LIMIT_PUBLIC_WINDOW_MS=60000
RATE_LIMIT_PUBLIC_MAX=10
